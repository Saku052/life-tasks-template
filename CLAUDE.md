# 会社PC用タスクボード

会社のタスクと私用のタスクを、このPCの1枚のカンバンで管理する。
Claude はこのファイルの規約に従って会社のタスクを操作する。

## 構成

| 場所 | 役割 |
|---|---|
| `office.py` | すべての操作の入口。Claude はこれを呼ぶ |
| `data/tasks.json` | **会社のタスク。このPCにだけ保存する**（git 管理外） |
| `config.json` | 私用タスクの秘密URLと合言葉（git 管理外。中身を表示しない） |
| `board/index.html` | ブラウザで開くカンバン（`./office.py open`） |
| `profile.md` | 会社での役割・案件名など、タイトル・ラベル・優先度の判定材料（各自で作る。git 管理外） |

## 私用タスクと会社タスクの扱い

| | 会社のタスク | 私用のタスク |
|---|---|---|
| 保存場所 | このPC（`data/tasks.json`） | 私用の GitHub（暗号化コピーを読むだけ） |
| このPCでの操作 | 追加・更新・完了すべてできる | **閲覧のみ** |
| 番号 | `W3` | `#12` |

- **私用タスクは変更しない。** 変更を頼まれたら「私用のスマホかPCから操作してください」と返す
- **会社のタスクをこのPCの外に出さない。** git への commit / push、外部サービスへの送信、私用側への転記はしない
- 私用タスクの中身を、会社のチャット・ドキュメント・コミットメッセージなどに書き写さない
- `config.json` の合言葉を表示・出力しない

## ステータス規約

| ステータス | 意味 | WIP上限 |
|---|---|---|
| **To do** | 未着手。いつかやる〜今週やるまで全部ここ | — |
| **Pending** | 自分以外の要因で止まっている / 意図的に寝かせた。**待ち対象を必ず `--reason` で残す** | 10 |
| **In progress** | 今まさに手を動かしている | 5 |
| **Done** | 完了条件を満たした | — |

WIP上限は会社と私用の合計で見る（手が回る量は1人分なので）。
「なんとなく手が止まった」は Pending ではなく **To do に戻す**。
時間軸の列（今週・今月）は作らない。時間は `due` が持つ。

## タスクの書き方

すべてのタスクは最低限これを持つ（`office.py add` が本文を組み立てる）。

```markdown
## 概要
なぜ今やるのか / 期限の根拠を含む1〜3行

## 完了に必要な手順
- [ ] 実行可能な単位に割った項目（3〜7個）
- [ ] 「調べる」で止めず、判断や成果物まで書く

## 完了条件
これが済んだら Done と言い切れる状態
```

- **タイトル**: 動詞で終える。20〜30文字。固有名詞を入れる（例:「週次定例の資料を作る」）
- **優先度**: 締切が3日以内 / 落とすと信用を失う → `P0`、今月内 / 誰かを待たせている → `P1`、それ以外 → `P2`
- **期限**:「〜までに」は前日、「今週中」は金曜、「今月中」は月末。推定したら概要に根拠を1行書く
- **ラベル**: `profile.md` の対応表に従う。無ければ案件名や「定例」「レビュー」「事務」など短い語で付ける
- 顧客名など社外秘に近い固有名詞も書いてよい（このPCから出ないため）

## Claude の操作ルール

- 起票・デイリーの前に `profile.md` があれば読む。判断材料がそろっていれば毎回聞き返さない
- タスク操作は **必ず `./office.py` 経由**。`data/tasks.json` を直接編集しない
- ステータスはユーザーの明示があったときだけ動かす。進捗を推測して変えない
- `done` はチェックリストが残っていると止まる。残項目を提示してから判断を仰ぐ
- 5分で終わる用事はタスクにしない

よく使うコマンド:

```bash
./office.py open                    # ボードをブラウザで開く
./office.py board                   # 会社 + 私用のカンバンを文字で見る（--json で機械可読）
./office.py add --title ... --summary ... --check ... --check ... [--dod ...] [--label ...] [--priority P1] [--due 2026-10-01]
./office.py start 3                 # In progress へ
./office.py move 3 pending --reason "先方の返事待ち"
./office.py check 3 "資料"           # チェックリストを消し込む
./office.py due 3 2026-10-01
./office.py done 3
./office.py note 3 "作業ログ"
./office.py stale 7                 # 放置検知（会社のタスク）
./office.py personal                # 私用タスクだけを見る
```

## スキル

| skill | いつ動く |
|---|---|
| `office-task-add` | 「タスク追加」「〜やらないと」 |
| `office-board` | 「ボード開いて」「進捗どう」「W3 やる」「終わった」「止まってる」「棚卸し」 |
| `office-daily` | 「おはよう」「今日なにやる」 |
